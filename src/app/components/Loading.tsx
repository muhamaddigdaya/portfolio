import React from 'react'

type Props = {}

const Loading = (props: Props) => {
  return (
    <div className='text-md font-extrabold animate-pulse m-10 text-black'>Loading...</div>
  )
}

export default Loading